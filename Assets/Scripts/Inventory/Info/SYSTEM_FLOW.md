# 🔄 Flujo del Sistema de Inventario

## 📊 Diagramas de Flujo

---

## 1. Flujo de Input

```
┌─────────────────────────────────────────┐
│  Usuario presiona tecla                 │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  Unity Input System                     │
│  - Lee Player.inputactions              │
│  - Detecta: Tab, E, ← →, Esc           │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  PlayerInputHandler                     │
│  - OnInventoryToggleInput()             │
│  - OnInventoryNavigateInput()           │
│  - OnInventoryInteractInput()           │
│  - OnInventoryCancelInput()             │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  InventoryUIController                  │
│  - Procesa según estado actual          │
│  - Valida acciones disponibles          │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  InventorySystem                        │
│  - Ejecuta lógica de datos              │
│  - Emite eventos                        │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  UI / Audio / Effects                   │
│  - Actualiza visuales                   │
│  - Reproduce sonidos                    │
└─────────────────────────────────────────┘
```

---

## 2. Máquina de Estados

```
                  ┌──────────┐
                  │  CLOSED  │
                  │ (juego   │
                  │  activo) │
                  └────┬─────┘
                       │
            ┌──────────┴──────────┐
            │   Tab presionado    │
            │   Time.timeScale=0  │
            └──────────┬──────────┘
                       │
                       ↓
                  ┌──────────┐
            ┌─────┤   OPEN   ├─────┐
            │     │ (navegar │     │
            │     │  items)  │     │
            │     └────┬─────┘     │
            │          │           │
        ┌───┴──┐       │       ┌───┴───┐
        │ Tab  │       │       │  Esc  │
        │ Esc  │       │       │       │
        └───┬──┘       │       └───┬───┘
            │          │           │
            │      ┌───┴────┐      │
            │      │   E    │      │
            │      │presiona│      │
            │      └───┬────┘      │
            │          ↓           │
            │    ┌──────────┐     │
            │    │ CONTEXT  │     │
            │    │   MENU   │     │
            │    │(seleccion│     │
            │    │ acción)  │     │
            │    └────┬─────┘     │
            │         │           │
            │     ┌───┴────┐      │
            │     │   E    │      │
            │     │ejecuta │      │
            │     └───┬────┘      │
            │         │           │
            │         ↓           │
            │    Vuelve OPEN      │
            │         │           │
            └─────────┴───────────┘
                      │
                      ↓
                 ┌──────────┐
                 │  CLOSED  │
                 │ Time.    │
                 │timeScale │
                 │   = 1    │
                 └──────────┘
```

---

## 3. Flujo Completo: Usar Poción

```
┌──────────────────────────────────────────────────────────┐
│  1. ESTADO: CLOSED (jugando normalmente)                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  2. Usuario presiona TAB                                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  3. PlayerInputHandler.OnInventoryToggleInput()          │
│     └─→ inventoryUIController.ToggleInventory()          │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  4. InventoryUIController                                │
│     - Cambia estado: CLOSED → OPEN                       │
│     - Time.timeScale = 0 (pausa)                         │
│     - Emite: OnStateChanged(Open)                        │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  5. UI recibe OnStateChanged                             │
│     - Muestra canvas de inventario                       │
│     - Resalta slot seleccionado                          │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  6. Usuario presiona → (navega a poción)                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  7. PlayerInputHandler.OnInventoryNavigateInput(1.0)     │
│     └─→ inventoryUIController.NavigateInventory(1.0)     │
│         └─→ inventorySystem.SelectNext()                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  8. InventorySystem                                      │
│     - selectedSlot++ (0 → 1)                             │
│     - Emite: OnSelectionChanged(0, 1)                    │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  9. UI recibe OnSelectionChanged                         │
│     - Mueve highlight al slot 1 (poción)                 │
│     - Muestra info de poción                             │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  10. Usuario presiona E                                  │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  11. PlayerInputHandler.OnInventoryInteractInput()       │
│      └─→ inventoryUIController.InteractWithCurrentItem() │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  12. InventoryUIController.OpenContextMenu()             │
│      - Lee CurrentItem (Health Potion)                   │
│      - Valida: item is IUsable? YES → add "Use"          │
│      - Valida: CanBeExamined? YES → add "Examine"        │
│      - Siempre: add "Drop"                               │
│      - availableActions = [Use, Examine, Drop]           │
│      - contextMenuIndex = 0                              │
│      - Cambia estado: OPEN → CONTEXT_MENU                │
│      - Emite: OnContextMenuOpened([Use, Examine, Drop])  │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  13. UI recibe OnContextMenuOpened                       │
│      - Muestra panel de menú contextual:                 │
│        ┌──────────────┐                                  │
│        │ ► Use        │ ← seleccionado                   │
│        │   Examine    │                                  │
│        │   Drop       │                                  │
│        └──────────────┘                                  │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  14. Usuario presiona E (ejecutar "Use")                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  15. PlayerInputHandler.OnInventoryInteractInput()       │
│      └─→ inventoryUIController.InteractWithCurrentItem() │
│          └─→ ExecuteContextAction()                      │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  16. InventoryUIController.ExecuteContextAction()        │
│      - action = availableActions[0] = Use                │
│      - switch(Use):                                      │
│        └─→ inventorySystem.UseCurrentItem()              │
│        └─→ CloseContextMenu()                            │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  17. InventorySystem.UseCurrentItem()                    │
│      - item = items[selectedSlot]                        │
│      - item.itemData.Use(gameObject)                     │
│        └─→ ConsumableItemData.Use()                      │
│            - health.Heal(healAmount)                     │
│            - return true (RemoveOnUse)                   │
│      - RemoveItemAt(selectedSlot, 1)                     │
│      - Emite: OnItemUsed(item)                           │
│      - Emite: OnItemRemoved(slot, item)                  │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  18. HealthController.Heal()                             │
│      - currentHealth += 50                               │
│      - Emite: OnHealed(50)                               │
│      - Console: "Player healed 50. Health: 100/100"      │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  19. UI recibe eventos                                   │
│      - OnItemRemoved: actualiza slot visual (vacío/qty)  │
│      - OnContextMenuClosed: cierra panel de menú         │
│      - Vuelve a mostrar inventario (estado OPEN)         │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  20. Usuario presiona TAB (cerrar inventario)            │
│      - inventoryUIController.ToggleInventory()           │
│      - Estado: OPEN → CLOSED                             │
│      - Time.timeScale = 1 (resume)                       │
│      - Emite: OnStateChanged(Closed)                     │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ↓
┌──────────────────────────────────────────────────────────┐
│  21. UI recibe OnStateChanged(Closed)                    │
│      - Oculta canvas de inventario                       │
│      - Juego continúa                                    │
└──────────────────────────────────────────────────────────┘
```

---

## 4. Construcción del Menú Contextual

```
┌─────────────────────────────────────────┐
│  Usuario presiona E sobre item          │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  InventoryUIController                  │
│  .OpenContextMenu()                     │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  1. Obtener item actual                 │
│     currentItem = inventorySystem       │
│                   .CurrentItem          │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  2. Limpiar lista                       │
│     availableActions.Clear()            │
│     contextMenuIndex = 0                │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  3. Validar "Use"                       │
│     if (item is IUsable usable)         │
│        if (usable.CanUse(gameObject))   │
│           → ADD "Use"                   │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  4. Validar "Examine"                   │
│     if (item.CanBeExamined)             │
│        → ADD "Examine"                  │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  5. Validar "Equip"                     │
│     if (item is WeaponItemData)         │
│        → ADD "EquipPrimary"             │
│        → ADD "EquipSecondary"           │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  6. Siempre añadir "Drop"               │
│     → ADD "Drop"                        │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  7. Cambiar estado y emitir evento      │
│     currentState = ContextMenu          │
│     OnContextMenuOpened?.Invoke(        │
│         availableActions)               │
└─────────────────────────────────────────┘


EJEMPLOS DE MENÚS GENERADOS:

┌─────────────────┬──────────────────┬──────────────────┐
│ HEALTH POTION   │ PISTOL           │ MASTER KEY       │
├─────────────────┼──────────────────┼──────────────────┤
│ ► Use           │ ► Equip Primary  │ ► Examine        │
│   Examine       │   Equip Secondary│   Drop           │
│   Drop          │   Examine        │                  │
│                 │   Drop           │                  │
└─────────────────┴──────────────────┴──────────────────┘

Si la salud está llena:

┌─────────────────┐
│ HEALTH POTION   │
├─────────────────┤
│ ► Examine       │  ← "Use" no aparece
│   Drop          │
└─────────────────┘
```

---

## 5. Flujo de Navegación

```
ESTADO: OPEN (inventario)
──────────────────────────

Input: ←
  ↓
OnInventoryNavigateInput(-1.0)
  ↓
NavigateInventory(-1.0)
  ↓
inventorySystem.SelectPrevious()
  ↓
selectedSlot = (selectedSlot - 1 + 6) % 6
  ↓
OnSelectionChanged(oldSlot, newSlot)


ESTADO: CONTEXT_MENU
──────────────────────────

Input: ↓
  ↓
OnInventoryNavigateInput(1.0)
  ↓
NavigateContextMenu(1.0)
  ↓
contextMenuIndex = (contextMenuIndex + 1) % availableActions.Count
  ↓
OnContextMenuSelectionChanged(contextMenuIndex)
```

---

## 6. Flujo de Validación

```
┌─────────────────────────────────────────┐
│  CanUse() validation flow               │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  ConsumableItemData.CanUse()            │
│  1. health = user.GetComponent<Health>()│
│  2. if (health == null) return false    │
│  3. if (health.CurrentHealth >=         │
│        health.MaxHealth) return false   │
│  4. return true                         │
└──────────────┬──────────────────────────┘
               │
               ↓
┌─────────────────────────────────────────┐
│  OpenContextMenu() usa resultado        │
│  - true  → "Use" aparece en menú        │
│  - false → "Use" NO aparece en menú     │
└─────────────────────────────────────────┘
```

---

## 7. Eventos del Sistema

```
┌──────────────────────────────────────────────┐
│           InventorySystem                    │
├──────────────────────────────────────────────┤
│  OnItemAdded(slot, item)                     │
│  OnItemRemoved(slot, item)                   │
│  OnItemUsed(item)                            │
│  OnSelectionChanged(oldIndex, newIndex)      │
│  OnInventoryFull()                           │
│  OnWeaponEquipped(slot, weapon)              │
│  OnWeaponUnequipped(slot)                    │
│  OnAmmoChanged(type, count)                  │
└──────────────┬───────────────────────────────┘
               │
               ↓
┌──────────────────────────────────────────────┐
│         InventoryUIController                │
├──────────────────────────────────────────────┤
│  OnStateChanged(state)                       │
│  OnContextMenuOpened(actions)                │
│  OnContextMenuClosed()                       │
│  OnContextMenuSelectionChanged(index)        │
└──────────────┬───────────────────────────────┘
               │
               ↓
┌──────────────────────────────────────────────┐
│        UI / Audio / Visual Systems           │
│  - Canvas                                    │
│  - Sprites                                   │
│  - Audio Source                              │
│  - Particle Effects                          │
└──────────────────────────────────────────────┘
```

---

## 8. Ciclo de Vida Completo

```
APPLICATION START
       │
       ↓
┌──────────────────┐
│ Awake()          │
│ - Get components │
└────┬─────────────┘
     │
     ↓
┌──────────────────┐
│ Start()          │
│ - Subscribe      │
│   events         │
└────┬─────────────┘
     │
     ↓
┌──────────────────────────────────────┐
│ GAME LOOP                            │
│ ┌────────────────────────────┐       │
│ │ Update()                   │       │
│ │ - Input System             │       │
│ │   detecta teclas           │       │
│ │ - Callbacks ejecutan       │       │
│ │ - UI actualiza             │       │
│ └────────────────────────────┘       │
│                                      │
│ EVENTOS:                             │
│ - OnItemAdded                        │
│ - OnStateChanged                     │
│ - OnContextMenuOpened                │
│ - etc.                               │
└────┬─────────────────────────────────┘
     │
     ↓
┌──────────────────┐
│ OnDestroy()      │
│ - Unsubscribe    │
│   events         │
└──────────────────┘
```

---

## 📌 Resumen de Flujos Clave

### Toggle Inventario
```
Tab → Toggle → SetState(Open/Closed) → Time.timeScale
```

### Navegar Items
```
← → → Navigate → SelectNext/Previous → OnSelectionChanged
```

### Abrir Menú
```
E → Interact → OpenContextMenu → Validate actions → OnContextMenuOpened
```

### Ejecutar Acción
```
E → Execute → switch(action) → Use/Examine/Drop/Equip → CloseContextMenu
```

### Cerrar
```
Esc → Cancel → CloseContextMenu/CloseInventory → SetState
```

---

**Este flujo garantiza:**
- ✅ Separación de responsabilidades
- ✅ Validación automática
- ✅ Estado consistente
- ✅ Feedback al usuario
- ✅ Extensibilidad futura
